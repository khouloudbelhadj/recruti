<!DOCTYPE html>

<title>Media</title>

{% block body %}
    <h1>Media</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ media.id }}</td>
            </tr>
            <tr>
                <th>Chemin</th>
                <td>{{ media.chemin }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ media.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_media_index') }}">back to list</a>

    <a href="{{ path('app_media_edit', {'id': media.id}) }}">edit</a>

    {{ include('media/_delete_form.html.twig') }}
{% endblock %}
