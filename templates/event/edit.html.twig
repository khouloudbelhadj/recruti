{% extends 'baseBack.html.twig' %}

{% block title %}<title>Edit Event</title>{% endblock %}

{% block body %}
    <div class="card" style="margin-left:40px;margin-top:25px;margin-right:40px;">
        <div class="card-header" align="center">
            <strong>Update</strong> Event
        </div>

        {{ include('event/_form.html.twig', {'button_label': 'Update'}) }}

        <div style="margin-left:400px; margin-top:-17px;">
                {{ include('event/_delete_form.html.twig') }}
            </div>
            <button class="btn btn-sm" style="background-color: white; margin-right:auto;"> 
                    <a href="{{ path('app_event_index') }}"><img  src="{{ asset('BackOffice/img/retour.png') }}" style="width: 30px; height: 30px;"></a>
                </button>
        </div>
{% endblock %}
