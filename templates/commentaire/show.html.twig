<!DOCTYPE html>

<title>Commentaire</title>

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Contenu_com</th>
                <td>{{ commentaire.contenuCom }}</td>
            </tr>
            <tr>
                <th>Date_creationCom</th>
                <td>{{ commentaire.dateCreationCom ? commentaire.dateCreationCom|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_edit', {'id': commentaire.id}) }}">edit</a>

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}
