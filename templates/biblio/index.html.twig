{% extends 'baseBack.html.twig' %}

{% block title %}<title>Library</title>{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">List of Libraries</h1>
        <a href="{{ path('app_biblio_new') }}" class="btn btn-success float-end" style="margin-block-end: 20px;">Create new</a>


        <table class="table table-borderless table-data3">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Name</th>
                    <th scope="col">Field</th>
                    <th scope="col">Creation Date</th>
                    <th scope="col">Actions</th>

                </tr>
            </thead>

            <tbody>
                {% for biblio in biblios %}
                    <tr>
                        <td>{{ biblio.id }}</td>
                        <td>{{ biblio.nomB }}</td>
                        <td>{{ biblio.domaineB }}</td>
                        <td>{{ biblio.dateCreationB ? biblio.dateCreationB|date('Y-m-d') : '' }}</td>
                        <td>
                         <a href="{{ path('app_biblio_show', {'id': biblio.id}) }}" class="btn btn-outline-secondary btn-sm">Show</a>
                         <a href="{{ path('app_biblio_edit', {'id': biblio.id}) }}" class="btn btn-outline-danger btn-sm">Edit</a>
                        </td>
                     </tr>
                 {% else %}
                     <tr>
                        <td colspan="9">No records found</td>
                     </tr>
                 {% endfor %}
             </tbody>
        </table>

    </div>
{% endblock %}
