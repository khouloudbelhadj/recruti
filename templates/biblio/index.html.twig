{% extends 'baseBack.html.twig' %}

{% block title %}<title>Library</title>{% endblock %}

{% block body %}
    <div class="container mt-4">
            <div class="card-header" align="center"><br>
               <h1 class="mb-4"style="color: #a90909;"><strong>List of Libraries</strong></h1>
            </div>   
<a href="{{ path('app_biblio_new') }}" class="btn float-end" style="margin-block-end: 20px; background-color: #a90909; color: white;">Add New Library</a>
    <form method="get" action="{{ path('app_biblio_index') }}" class="mb-3">
        <div class="input-group">
            <input type="text" name="nameSearch" class="form-control" placeholder="Search by Name...">
            <input type="text" name="fieldSearch" class="form-control" placeholder="Search by Field...">
            <input type="text" name="dateSearch" class="form-control" placeholder="Search by Date...">
            <button type="submit" class="btn btn-primary" style="background-color: #a90909;">Search</button>
        </div>
    </form>

        <table class="table table-borderless table-data3">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Name</th>
                    <th scope="col">Field</th>
                    <th scope="col">Creation Date</th>
                    <th scope="col">Image</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>

            <tbody>
                {% for biblio in biblios %}
                {% set img = biblio.imageB %}

                    <tr>
                        <td>{{ biblio.id }}</td>
                        <td>{{ biblio.nomB }}</td>
                        <td>{{ biblio.domaineB }}</td>
                        <td>{{ biblio.dateCreationB ? biblio.dateCreationB|date('Y-m-d') : '' }}</td>
                        <td><img style="width:150px;height:150px;" src="{{asset(img) }}"></td>

                        <td>
                            <a href="{{ path('app_biblio_show', {'id': biblio.id}) }}" class="btn btn-outline-secondary btn-sm">Show</a>
                            <a href="{{ path('app_biblio_edit', {'id': biblio.id}) }}" class="btn btn-outline-danger btn-sm">Edit</a>
                            <a href="{{ path('app_ressource_new', {'id': biblio.id}) }}" class="btn btn-outline-danger btn-sm">Add Resources</a>

                        </td>
                    </tr>
                {% else %}
                     <td colspan="10" style="text-align: center; color: #a90909;"><strong>No records found</strong></td>
                {% endfor %}
            </tbody>
        </table>

    </div><br><br>
{% endblock %}
